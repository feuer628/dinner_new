export enum RoleActions {
    SYSTEM_ADMIN = 1,
    ORDER_FOR_OTHER = 2,
    ADD_ORG = 3,
    MANAGE_ORG_GROUP = 4,
    CONFIRM_ALL_USERS = 5,
    CONFIRM_ORG_USERS = 6,
    MANAGE_RIGHTS_ORG = 7
}

export enum Status {
    NEW = 0,
    CONFIRMED = 1,
    DELETED = 2
}